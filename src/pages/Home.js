const Home = () =>{  

    return(  
        <div class="homeMainHeading"> 
            <h1>Name</h1>
        </div>
    )
}

// idea 
// have a state that shows if the hamburger menu is open or not


export default Home